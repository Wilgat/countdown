# Report: timer (immediate bootstrap origin) — post chain-fix

**Date:** 2026-07-16  
**Mode:** bootstrap origin / intermediate hop review  
**Ship unit:** `./timer` (workspace reference; may be gitignored on countdown product publish)  
**VERSION:** `2.9.0`  
**Role on chain:** **Immediate bootstrap origin** of countdown; specialized from selfmanaged  
**Direction:** A → B only  
**Lessons re-checked:** L-01…L-09, L-13 (+ Type 0 L-03/L-04/L-06/L-07)  
**Prior inputs:** `2026-07-15-bootstrap-origin.md`, `2026-07-16-bootstrap-chain-fix.md`  
**Status:** **Revise** (prior origin **bugs closed in code**; residual suggestions/docs/tests)

## Summary

Timer carries Type 0 lifecycle plus **count-up named timer** domain. The 2026-07-15 origin review **Blocked** on inherited bugs; the 2026-07-16 chain fix closed those bugs on `./timer` in place (not by reverse-copying countdown). Domain storage is now private per-user dirs; already_running uses stderr JSON errors; stop/kill fail closed on `rm`; exclusive create and integer state checks are present. Remaining gaps: nested list JSON string shape (L-10), optional stricter TP automation, and any timer-only suite not run from this workspace.

## Scope

| Field | Value |
|-------|--------|
| Product type | Type 0 + domain (elapsed timers) |
| Domain verbs | start / stop / status / list / kill / reset (+ `--persist`) |
| Chain position | Intermediate / immediate parent of countdown |
| Parent | selfmanaged (root) |
| Child | countdown (leaf) |

## Lessons re-check (this hop)

| ID | Result | Evidence (summary) |
|----|--------|--------------------|
| L-01 | **Pass** | `out_json_error` for already_running |
| L-02 | **Pass** | `${VOLATILE_DIR}/${APP_NAME}-${USERNAME}` + chmod 700 |
| L-03 | **Pass** | `inst_get_version` user-local prefer |
| L-04 | **Pass** | no blanket `.local/bin` sed |
| L-05 | **Pass** | rm fail / residual file → `io_error` |
| L-06 | **Pass** | shared Type 0 escape + CR/LF name reject (length/tr) |
| L-07 | **Pass** | wget `HTTP/[0-9.]+ 200` |
| L-08 | **Pass** | integer case on state before arithmetic |
| L-09 | **Pass** | `set -C` exclusive create |
| L-10 | **Open** | `timers` still string-encoded via `out_json` |
| L-13 | **Pass** | `list` rejects free name argument |

## Issues

### Closed on this hop (were open pre-2026-07-16)

| Former leaf # | Severity | Topic | Status |
|---------------|----------|-------|--------|
| 1 | bug | already_running JSON channel | **closed** |
| 2 | bug | shared volatile storage | **closed** |
| 3 | bug | inst_get_version | **closed** |
| 4 | bug | PATH sed | **closed** |
| 5 | bug | rm success without check | **closed** |
| 6 | bug | JSON escape / name controls | **closed** |
| 7 | suggestion | wget HTTP/1.1 | **closed** |
| 8 | suggestion | non-numeric state | **closed** |
| 9 | suggestion | start TOCTOU | **closed** |
| 14 | nit | list free token | **closed** (reject) |

### Still open / residual (timer)

### Issue 1 -- Severity: suggestion
- File: timer (list JSON path)
- Description: `timer_list` still builds a nested array as a string field through string-only `out_json` (L-10). Machines need a second parse.
- Suggestion: Document contract or extend emitter for raw array fields; escape each name element-wise.
- Lesson: L-10
- Status: open

### Issue 2 -- Severity: suggestion
- File: timer tests (if present outside this repo’s `tests/`)
- Description: Countdown’s suite does not execute timer domain; regression relies on shared lessons + manual/origin re-review unless timer has its own suite.
- Suggestion: Maintain timer CI for domain edges (already_running stderr, private dir, list reject) or document that leaf TP rows are the only automated lock-in.
- Test: TP-A1 / TP-A2
- Status: open

### Issue 3 -- Severity: nit
- File: timer VERSION / publish
- Description: Timer VERSION is `2.9.0` (legitimate for this hop). Do not “sync” it to countdown’s `1.1.0` by reverse-copying leaf identity.
- Suggestion: Bump timer only on intentional timer releases; keep channels distinct.
- Status: open (process honesty)

## Verdict

**Revise** — Prior **Block**-level bugs on timer are **closed in code**. Remaining work is L-10 list JSON shape, test ownership, and publish hygiene. No reverse-copy.

## Chain note

Timer remains the **default origin-review hop** for countdown leaf reports (L-B3). Shared Type 0 still preferably fixed on selfmanaged first when both need the same helper change.
